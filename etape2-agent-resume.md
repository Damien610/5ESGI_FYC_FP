# 🚀 MCP 

1-lancer le serveur
    cd mcp
    installer les deépendances : npm install express ollama
    lancer le serveur en mode dev ( reload ) npm run dev
    curl  http://localhost:3000 pour tester 

2-